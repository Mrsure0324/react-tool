---
description: 功能开发流程规范，当用户要求开发新功能、新需求时自动加载
globs:
alwaysApply: false
---

# 功能开发流程

## 触发条件

当用户要求开发新功能、实现新需求、或输入 `/feat` 指令时，遵循此流程。

## 阶段一：需求要素收集

在开始任何分析之前，确认具备以下必要输入：

1. **需求描述** — 清晰说明要做什么（必须）
2. **API 文档** — 涉及接口时必须有请求/响应结构
3. **UI 设计稿** — 涉及界面时必须有视觉参考（.pen 文件或截图）

缺少任何必要要素时，**主动询问补全，不要猜测**。

### API 文档位置

- 服务端 API 路由：`src/api/` 目录
- 客户端 API 封装：`src/services/` 目录
- 如果用户提供了接口文档链接或截图，先读取并提取关键字段

### 设计稿位置

- Pencil 设计稿：`pen/` 目录（通过 Pencil MCP 工具读取）
- 如需从 .pen 提取信息：使用 `batch_get` 读取结构，`get_screenshot` 查看视觉

## 阶段二：分析出方案

在写任何代码之前，完成以下分析：

1. **读取 API 文档** → 提取接口、字段、请求/响应类型
2. **分析现有代码** → 评估复用 vs 新建
3. **对比设计稿** → 找出现有组件能否满足，差异在哪
4. **输出执行计划**：

```
## 执行计划

### 文件清单
| 操作 | 文件路径 | 说明 |
|------|---------|------|
| 新建 | src/pages/xxx/index.tsx | 页面入口 |
| 新建 | src/pages/xxx/index.less | 页面样式 |
| 修改 | .umirc.ts | 新增路由 |
| ... | ... | ... |

### 关键决策
- 状态管理：useModel / Redux / 本地 useState
- 样式方案：LESS / CSS Module
- 复用组件：[列出可复用的现有组件]
- 新建组件：[列出需要新建的组件]
```

## 阶段三：等待确认

**方案必须经过用户确认后才写代码。** 原因：
- 避免方向错误导致返工
- 用户可以补充 AI 不知道的业务上下文
- 建立信任感

输出方案后等待用户回复。用户说"可以"、"开始"、"确认"等肯定词后才进入执行。

## 阶段四：执行

### 执行规则

1. 使用 TODO 追踪每个文件的进度
2. 并行修改无依赖的文件
3. 新页面必须同步更新 `.umirc.ts` 路由配置
4. 新的全局状态必须在 `src/models/` 创建对应文件
5. 新的 API 封装放到 `src/services/` 中
6. 类型定义就近放置（当前模块的 `types.ts`），或全局的放 `src/types/`

### 代码生成 Checklist

- [ ] 组件使用箭头函数 + export default
- [ ] Props 用 interface 定义
- [ ] 导入使用 `@/` 路径别名
- [ ] 样式使用 LESS（`import styles from './index.less'`）
- [ ] 页面使用 `PageContainer` 包裹
- [ ] 路由已在 `.umirc.ts` 注册
- [ ] TypeScript 类型完整，无 any
- [ ] 异步数据有 Loading / Error / Empty 三态处理

## 阶段五：收尾

- 检查 lint 错误并修复
- 输出改动总结（新建了什么文件、修改了什么文件）
- 提示用户验证步骤
