---
description: 快捷指令映射表，识别用户简短指令并执行对应操作
globs:
alwaysApply: true
---

# 快捷指令

当用户输入以下简短指令时，执行对应操作：

| 指令 | 含义 |
|------|------|
| `继续` / `c` | 重新执行上一步未完成的操作 |
| `重试` / `r` | 上一步失败，用相同方案重试 |
| `撤销` / `u` | 撤销上一步的代码改动 |
| `回滚` / `rb` | 回滚到最近的正常状态（可能撤销多步） |
| `/feat` | 触发功能开发流程（加载 feature-dev 规则） |
| `/review` | 对当前文件或选中代码进行 Code Review |
| `/page` | 生成新页面（脚手架 + 路由注册） |
| `/refactor` | 重构优化当前代码 |
| `/component` | 触发组件开发流程（加载 component-dev 规则） |
| `/fix` | 触发 Bug 修复流程（加载 bugfix 规则） |

## 使用方式

用户可以直接输入指令本身，或在指令后附加描述：

```
/feat 用户登录页面
/review
/page 订单详情
/fix 列表页空白
/component 搜索框
```
