---
description: Bug 修复流程规范，当用户报告 bug、排查问题、或输入 /fix 指令时自动加载
globs:
alwaysApply: false
---

# Bug 修复流程

## 核心原则

**禁止猜测修改。必须定位后再动手。**

## 阶段一：信息收集

确认以下信息（缺的主动问）：

| 信息 | 说明 | 必须？ |
|------|------|--------|
| 现象描述 | 什么表现？看到了什么？ | ✅ |
| 复现路径 | 操作步骤 | ✅ |
| 影响范围 | 哪个页面、哪个组件 | ✅ |
| 报错信息 | 控制台日志、错误截图 | 尽量 |
| 预期行为 | 正确应该是什么 | 尽量 |

## 阶段二：定位分析

1. **读相关源码** — 按用户描述找到对应文件
2. **追踪数据流** — 用户操作 → 事件回调 → 状态变更 → 渲染
3. **检查常见坑位**：
   - Umi 路由配置（`.umirc.ts`）是否正确
   - `useModel` 的 model 名是否对应文件名
   - API 接口路径是否正确
   - 样式类名是否拼错（LESS 非 CSS Module 不会报错）
   - 异步操作的竞态条件
   - `useEffect` 依赖数组是否遗漏
4. **提出假设**："问题出在 X，因为 Y"
5. **给出修复方案和影响范围评估**

## 阶段三：确认后修复

- 等用户确认方案后再改代码
- **修复范围最小化**，不顺便重构
- 需要重构的记录下来，单独处理
- 如果修复涉及多个文件，用 TODO 列表追踪

## 阶段四：复盘沉淀

修复完成后思考：

1. **这个坑会不会再踩？** → 如果会，更新 `gotchas.mdc` 的迭代记录表格
2. **AI 之前有没有生成过类似有问题的代码？** → 如果有，补充规则避免复发
3. **告知验证方式** → 告诉用户如何确认 bug 已修复

### 更新踩坑记录模板

```markdown
| {日期} | {问题简述} | {根因} | {修复方式} |
```

添加到 `.cursor/rules/gotchas.mdc` 的迭代记录表格中。
